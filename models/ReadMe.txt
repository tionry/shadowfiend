/后端tips

/有任何疑惑，官网永远是第一选择
    http://www.mongodb.org/

/资源锁：
    mongoDB作为一种非关系型数据库，它的锁机制曾为人诟病，2.2版本以前mongo的锁是全局级别的，这确实是令人不快的一点，2.2以后锁的粒度减小到
    库集锁，不能像Oracle一样能支持行集锁，不过不用过于担心并发量的问题。
    之所以提到这一点是想要解释其实在后端借口中没有必要写资源锁，简单的增删改查mongo完全可以应对，比较极端会出现使资源锁阻塞的情况，但是在这项
    工程中完全不必担心。

/注意js代码阻塞情况